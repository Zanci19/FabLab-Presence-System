# FabLab-Presence-System

## Animation configuration

You can enable/disable animations globally via `settings.json`:

```json
{
  "animationsEnabled": true
}
```

Set `animationsEnabled` to `false` to turn off intro typing, screen transitions, and all CSS animations/transitions.
